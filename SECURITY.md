# 安全策略

## 报告安全问题

请不要在公开 Issue 中披露尚未修复的漏洞、真实凭据、个人信息或可直接利用的攻击步骤。
优先使用 GitHub 仓库的 Private vulnerability reporting 或 Security Advisory 私密报告功能。

报告应包含受影响版本、复现条件、预期影响和不含秘密的最小证据。请使用占位符替代
Token、API Key、Cookie、私钥、内部域名和本机路径。

## 支持范围

安全修复以最新 `main` 为主要支持目标。早期预览版本可能发生不兼容变更。

## 凭据处理

- 真实秘密只能来自运行时环境、受保护文件或平台 Secret，不得写入源码和前端变量。
- 发现凭据误提交后，应先吊销或轮换凭据，再处理 Git 历史。
- 日志、Trace、Capsule 和错误响应必须避免输出凭据或完整认证头。
