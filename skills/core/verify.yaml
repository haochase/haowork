name: verify
version: 1.0.0
input_schema: '{"type":"object","additionalProperties":false,"properties":{"evidence_id":{"type":"string"}},"required":["evidence_id"]}'
output_schema: '{"type":"object","additionalProperties":false,"properties":{"verified":{"type":"boolean"}},"required":["verified"]}'
risk: L1
allowed_functions: [verify]
required_context: true
required_lease: true
supported_environments: [public, internal]
adapter: core.verify
timeout: 60s
retry_policy: workflow-owned
evidence_policy: independent-verification
