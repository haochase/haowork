name: import
version: 1.0.0
input_schema: '{"type":"object","additionalProperties":false,"properties":{"capsule_sha256":{"type":"string"}},"required":["capsule_sha256"]}'
output_schema: '{"type":"object","additionalProperties":false,"properties":{"preview_id":{"type":"string"}},"required":["preview_id"]}'
risk: L3
allowed_functions: [manager, delivery_leader]
required_context: true
required_lease: true
supported_environments: [internal]
adapter: core.import
timeout: 90s
retry_policy: workflow-owned
evidence_policy: owner-confirmation
