name: context
version: 1.0.0
input_schema: '{"type":"object","additionalProperties":false,"properties":{"task_id":{"type":"string"}},"required":["task_id"]}'
output_schema: '{"type":"object","additionalProperties":false,"properties":{"context_id":{"type":"string"}},"required":["context_id"]}'
risk: L0
allowed_functions: [manager, delivery_leader, research, build, verify]
required_context: false
required_lease: false
supported_environments: [public, internal]
adapter: core.context
timeout: 30s
retry_policy: workflow-owned
evidence_policy: context-hash
